{
  "name": "layk-platform",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.10.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "build:web": "turbo build --filter=web"
  },
  "devDependencies": {
    "turbo": "^2.5.4"
  }
}
