{
  "name": "layk-platform",
  "private": true,
  "version": "0.0.0",
  "packageManager": "npm@11.10.1",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo build --filter=web",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "build:web": "turbo build --filter=web"
  },
  "devDependencies": {
    "expo": "~54.0.0",
    "turbo": "^2.5.4"
  },
  "overrides": {
    "react": "19.2.7",
    "react-native": "0.81.5"
  }
}
